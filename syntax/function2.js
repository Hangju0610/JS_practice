console.log(Math.round(1.6)); // 2 / round = 반올림해주는 함수
console.log(Math.round(1.4)); // 1

function sum(first, second){ // 매개변수, parameter
    return first+second; // return은 어떤 값을 출력한다는 의미와 즉시 함수를 종료한다는 의미

}

console.log(sum(2,4)); // argument : 인자